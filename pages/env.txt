

	env
	
	Shows all the settings your terminal is currently using.
	More information: https://manned.org/env.


		- See every setting:
		env

		- Look for one in particular:
		env | grep PATH

		- Run a command with one setting changed:
		env EDITOR=nano git commit


